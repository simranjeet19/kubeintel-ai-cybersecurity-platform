apiVersion: v1
kind: Service
metadata:
  name: summarizer-svc
  namespace: kubeintel
  labels:
    app: summarizer-api
spec:
  type: ClusterIP
  selector:
    app: summarizer-api
  ports:
    - port: 8000
      targetPort: 8000
