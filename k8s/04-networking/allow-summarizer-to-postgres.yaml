apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-summarizer-to-postgres
  namespace: kubeintel
spec:
  podSelector:
    matchLabels:
      app: postgres
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: summarizer-api
      ports:
        - protocol: TCP
          port: 5432
