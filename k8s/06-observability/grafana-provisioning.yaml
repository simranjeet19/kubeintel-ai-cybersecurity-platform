apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-provisioning
  namespace: kubeintel
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-svc:9090
        access: proxy
        isDefault: true
