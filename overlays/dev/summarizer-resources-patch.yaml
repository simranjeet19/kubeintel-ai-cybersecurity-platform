apiVersion: apps/v1
kind: Deployment
metadata:
  name: summarizer-api
  namespace: kubeintel
spec:
  template:
    spec:
      containers:
        - name: summarizer-api
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
